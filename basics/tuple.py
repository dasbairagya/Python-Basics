#in list we can change or assign the value i.e mutable but in tuple we can not change or assign the value i.e immutable hence tuple is faster than list. list use [] and tuple use ()
tup = (10, 20, 30, 54, 11, 22, 33)
print(tup[1])
